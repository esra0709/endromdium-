This directory receives the tables and figures produced by the analysis code.

It is empty in the repository; the files appear when the notebook and the scripts in
`analysis/` are run, in the order given in the top-level README.

`.gitignore` excludes generated `.csv` and `.npz` files, so results are not committed.
