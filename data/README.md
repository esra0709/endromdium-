This directory is intentionally empty.

The patient-level data are pseudonymized and are not distributed with this repository.
See "Data availability" in the top-level README.

`.gitignore` excludes this directory's contents, so a data file placed here will not be
committed by accident.
